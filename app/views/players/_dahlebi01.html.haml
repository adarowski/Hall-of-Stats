- content_for(:header_styles) do
  %style{:type => 'text/css'}
    :sass
      #branding
        border-top: 1px solid fade_out(black, 0.4)
        background-color: fade_out(black, 0.5)
      #photo-hint
        background-color: fade_out(black, 0.4)


= image_tag 'photos/dahlebi01.jpg', alt: 'Bill Dahlen Photo', width: '480'
%span#photo-hint
  That’s
  = succeed "." do
    = link_to "Bill Dahlen", player_path('dahlebi01')
